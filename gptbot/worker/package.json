{
  "name": "ask-this-site-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test worker.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
