window.StrategyEngine=function(){'use strict';let _deps=null,_active=!1,_fn=null,_ctx={},_crossIdx=0,_crossTick=0,_crossState=new Map,_crossLastTick=new Map;function _buildEnv(){const d=_deps;return{get open(){const o=d.getOhlc();return o.length?o[o.length-1].o:d.getPrice()},get high(){const o=d.getOhlc();return o.length?o[o.length-1].h:d.getPrice()},get low(){const o=d.getOhlc();return o.length?o[o.length-1].l:d.getPrice()},get close(){return d.getPrice()},get volume(){const v=d.getVolumeHistory();return v.length?v[v.length-1]:0},sma(src,len){const arr=d.getPriceHistory().slice(-len);return arr.length>=len?arr.reduce((a,b)=>a+b,0)/len:d.getPrice()},ema(src,len){const arr=d.getPriceHistory().slice(-Math.max(len*3,len+10));if(arr.length<len)return d.getPrice();const k=2/(len+1);let e=arr[0];for(let i=1;i<arr.length;i++)e=(arr[i]-e)*k+e;return e},crossover(a,b){const idx=_crossIdx++;const s=_crossState.get(idx);const result=null!=s&&_crossLastTick.get(idx)===_crossTick-1&&s.a<=s.b&&a>b;_crossState.set(idx,{a,b});_crossLastTick.set(idx,_crossTick);return result},crossunder(a,b){const idx=_crossIdx++;const s=_crossState.get(idx);const result=null!=s&&_crossLastTick.get(idx)===_crossTick-1&&s.a>=s.b&&a<b;_crossState.set(idx,{a,b});_crossLastTick.set(idx,_crossTick);return result},rsi(len=14){const arr=d.getPriceHistory().slice(-Math.max(len*3,len+10));if(arr.length<2)return 50;const k=1/len,init=Math.min(len,arr.length-1);let uA=0,dA=0;for(let i=1;i<=init;i++){const dv=arr[i]-arr[i-1];uA+=Math.max(dv,0);dA+=Math.max(-dv,0)}uA/=init;dA/=init;for(let i=init+1;i<arr.length;i++){const dv=arr[i]-arr[i-1];uA=uA*(1-k)+Math.max(dv,0)*k;dA=dA*(1-k)+Math.max(-dv,0)*k}return 0===dA?0===uA?50:100:100-100/(1+uA/dA)},atr(len=14){const ohlc=d.getOhlc();if(ohlc.length<2)return 0;const sl=ohlc.slice(-Math.max(len*3,len+10));if(sl.length<2)return sl[0]?sl[0].h-sl[0].l:0;const k=1/len;let v=sl[1].h-sl[1].l;for(let i=1;i<sl.length;i++){const tr=Math.max(sl[i].h-sl[i].l,Math.abs(sl[i].h-sl[i-1].c),Math.abs(sl[i].l-sl[i-1].c));v=v*(1-k)+tr*k}return v},highest(len=20){const ohlc=d.getOhlc(),end=ohlc.length-1,sl=ohlc.slice(Math.max(0,end-len),end);return sl.length?Math.max(...sl.map(b=>b.h)):d.getPrice()},lowest(len=20){const ohlc=d.getOhlc(),end=ohlc.length-1,sl=ohlc.slice(Math.max(0,end-len),end);return sl.length?Math.min(...sl.map(b=>b.l)):d.getPrice()},bb(len=20,mult=2){const price=d.getPrice(),arr=d.getPriceHistory().slice(-len);if(arr.length<len)return{upper:price,mid:price,lower:price};const mid=arr.reduce((a,b)=>a+b,0)/len,std=Math.sqrt(arr.reduce((s,v)=>s+(v-mid)**2,0)/len);return{upper:mid+mult*std,mid,lower:mid-mult*std}},stoch(kLen=14,dLen=3){const ohlc=d.getOhlc();if(ohlc.length<kLen)return{k:50,d:50};const calcK=bars=>{const hh=Math.max(...bars.map(b=>b.h)),ll=Math.min(...bars.map(b=>b.l));return hh===ll?50:(bars[bars.length-1].c-ll)/(hh-ll)*100},k=calcK(ohlc.slice(-kLen));let dSum=0,dCnt=0;for(let i=0;i<dLen;i++){const end2=ohlc.length-i;if(end2-kLen<0)break;dSum+=calcK(ohlc.slice(end2-kLen,end2));dCnt++}return{k,d:dCnt?dSum/dCnt:k}},get bar_index(){return Math.max(0,d.getOhlc().length-1)},strategy:{entry(direction,opts={}){if(!_active)return;const dir='long'===direction.toLowerCase()?1:-1;let hasSame=!1;const oppositeIds=[];for(const o of d.getOrders())o.dir===dir?hasSame=!0:oppositeIds.push(o.id);oppositeIds.forEach(id=>d.closeOrderById(id,!1));if(!hasSame){const lev=d.getLeverage(),frac=d.fractional();let shares;if(null!=opts.qty)shares=frac?opts.qty:Math.floor(opts.qty);else{const ratio=null!=opts.ratio?Math.min(1,Math.max(0,opts.ratio)):1,budget=ratio*d.getFreeCash()-d.FEE_FIXED,raw=budget/(d.getPrice()*(1/lev+d.FEE_RATE));shares=frac?raw:Math.floor(raw)}const sl=null!=opts.sl?opts.sl:null,tp=null!=opts.tp?opts.tp:null;shares>0&&d.placeOrder(dir,shares,sl,tp)}},add(direction,opts={}){if(!_active)return;const dir='long'===direction.toLowerCase()?1:-1,lev=d.getLeverage(),frac=d.fractional();let shares;if(null!=opts.qty)shares=frac?opts.qty:Math.floor(opts.qty);else{const ratio=null!=opts.ratio?Math.min(1,Math.max(0,opts.ratio)):1,budget=ratio*d.getFreeCash()-d.FEE_FIXED,raw=budget/(d.getPrice()*(1/lev+d.FEE_RATE));shares=frac?raw:Math.floor(raw)}const sl=null!=opts.sl?opts.sl:null,tp=null!=opts.tp?opts.tp:null;shares>0&&d.placeOrder(dir,shares,sl,tp)},close(direction){if(!_active)return;const dir='long'===direction.toLowerCase()?1:-1;[...d.getOrders().filter(o=>o.dir===dir).map(o=>o.id)].forEach(id=>d.closeOrderById(id,!1))},closeAll(){if(!_active)return;[...d.getOrders().map(o=>o.id)].forEach(id=>d.closeOrderById(id,!1))},get position(){const price=d.getPrice();let lQty=0,sQty=0,lSum=0,sSum=0,lU=0,sU=0;for(const o of d.getOrders()){const u=o.dir*o.shares*(price-o.openPrice);1===o.dir?(lQty+=o.shares,lSum+=o.openPrice*o.shares,lU+=u):(sQty+=o.shares,sSum+=o.openPrice*o.shares,sU+=u)}return lQty>0&&0===sQty?{side:'long',qty:lQty,avgPrice:lSum/lQty,unrealized:lU}:sQty>0&&0===lQty?{side:'short',qty:sQty,avgPrice:sSum/sQty,unrealized:sU}:{side:'none',qty:0,avgPrice:0,unrealized:0}}}}function init(deps){_deps=deps}function compile(code){try{return new Function('pineEnv',`with(pineEnv){${code}}`)}catch(e){return _deps.showToast('Strategy compile error: '+e.message,'error'),null}}function start(code){const fn=compile(code);if(!fn)return!1;_ctx={};_crossState.clear();_crossLastTick.clear();_crossTick=0;_fn=fn;return _active=!0,!0}function stop(){_active=!1}function reset(){_active=!1;_fn=null;_ctx={};_crossState.clear();_crossLastTick.clear();_crossTick=0;_crossIdx=0}function tick(){if(_active&&_fn){_crossTick++;_crossIdx=0;try{_fn.call(_ctx,_buildEnv())}catch(e){console.warn('Strategy execution error',e);_deps.showToast('Strategy execution error, stopped: '+e.message,'error');_active=!1;_deps.onStop&&_deps.onStop()}_deps.onAfterTick()}}function isActive(){return _active}return{init,compile,start,stop,reset,tick,isActive}}();